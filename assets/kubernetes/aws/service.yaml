apiVersion: v1
kind: Service
metadata:
  name: &app-name svc-go-inventory
  namespace: test-a
  labels:
    app: *app-name
spec:
  type: ClusterIP
  ports:
  - port: 7000
    targetPort: 7000
    protocol: TCP
    name: http
  selector:
    app: go-inventory
